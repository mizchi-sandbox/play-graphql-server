# Typse
type Shop {
  id: ID!
  name: String!
}

input ShopInput {
  name: String!
}

type Mutation {
  addShop(input: ShopInput): Shop
}

type Query {
  shops: [Shop]!
  lastShop: Shop
  findShopByName(shopName: String!): Shop
}
